name: 📸 Captura Radar Meteorológico

      # 2. Instalar herramientas nativas
      - name: 📦 Instalar wkhtmltopdf
        run: |
          echo "📦 Instalando wkhtmltopdf..."
          sudo apt-get update -qq
          sudo apt-get install -y wkhtmltopdf xvfb
          echo "✅ wkhtmltopdf instalado correctamente"
          
          echo "🔍 Verificando instalación..."
          wkhtmltoimage --version
          
          echo "🔧 Configurando Xvfb para modo headless..."
          export DISPLAY=:99
          Xvfb :99 -screen 0 1920x1080x24 &
          sleep 2
          echo "✅ Entorno configurado"       # 3. Verificar estructura de directorios
      - name: 📁 Verificar directorios
        run: |
          echo "🗋 Verificando estructura del proyecto..."
          mkdir -p captures
          echo "📁 Contenido de captures/:"
          ls -la captures/ || echo "Carpeta captures vacía"
          
          echo "🔍 Verificando archivos del proyecto:"
          find . -maxdepth 2 -type f -name "*.js" -o -name "*.json" -o -name "*.yml" | sort
          
          echo "📊 Espacio en disco disponible:"
          df -h .
          
          echo "🌍 Verificando conectividad:"
          curl -I https://www.windy.com || echo "Problema de conectividad"
  # 🚫 Ejecución automática temporalmente desactivada para testing
  # schedule:
  #   - cron: '*/5 * * * *'
  
  # Permite ejecución manual desde la interfaz de GitHub
  workflow_dispatch:
    inputs:
      cleanup_old_files:
        description: 'Limpiar archivos antiguos (mantener últimos 100)'
        required: false
        default: 'true'
        type: boolean

# Configuración de permisos
permissions:
  contents: write
  actions: read

jobs:
  capture-radar:
    name: 🌦️ Capturar Radar de Windy.com
    runs-on: ubuntu-latest
    
    # Timeout para evitar ejecuciones colgadas
    timeout-minutes: 10
    
    steps:
      # 1. Checkout del repositorio
      - name: 📥 Checkout repositorio
        uses: actions/checkout@v4
        with:
          # Traer todo el historial para commits
          fetch-depth: 0
          # Token para poder hacer push
          token: ${{ secrets.GITHUB_TOKEN }}

      # 2. Configurar Node.js
      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # No usar cache hasta que tengamos package-lock.json estable
          cache: 'npm'
          cache-dependency-path: 'package.json'

      # 3. Instalar dependencias
      - name: 📦 Instalar dependencias
        run: |
          echo "� Instalando dependencias con npm install..."
          npm install
          echo "✅ Dependencias instaladas correctamente"

      # 4. Verificar estructura de directorios
      - name: 📁 Verificar directorios y dependencias
        run: |
          echo "🗋 Verificando estructura del proyecto..."
          mkdir -p captures
          echo "📁 Contenido de captures/:"
          ls -la captures/ || echo "Carpeta captures vacía"
          
          echo "📦 Verificando package.json:"
          cat package.json | head -20
          
          echo "🔍 Verificando archivos del proyecto:"
          find . -maxdepth 2 -type f -name "*.js" -o -name "*.json" -o -name "*.yml" | sort
          
          echo "📊 Espacio en disco disponible:"
          df -h .

      # 4. Ejecutar script de captura
      - name: 📸 Ejecutar captura de radar
        run: |
          echo "🚀 Iniciando captura del radar meteorológico..."
          echo "🔧 Verificando script de captura..."
          
          if [ ! -f "scripts/screenshot.js" ]; then
            echo "❌ Error: No se encuentra scripts/screenshot.js"
            exit 1
          fi
          
          echo "� Verificando wkhtmltoimage..."
          wkhtmltoimage --version
          
          echo "✅ Script encontrado, ejecutando captura..."
          export DISPLAY=:99
          node scripts/screenshot.js
          
          echo "📸 Captura completada, verificando resultados..."
          ls -la captures/ || echo "No se generaron capturas"

      # 5. Verificar si hay nuevos archivos
      - name: 🔍 Verificar cambios
        id: verify-changes
        run: |
          echo "🔍 Verificando estado del repositorio..."
          
          # Mostrar estado actual
          echo "📁 Estado de Git:"
          git status
          
          echo "📈 Contenido actual de captures/:"
          ls -la captures/ || echo "Carpeta captures vacía"
          
          # Contar archivos en captures
          FILE_COUNT=$(find captures -name "*.png" 2>/dev/null | wc -l)
          echo "file_count=$FILE_COUNT" >> $GITHUB_OUTPUT
          echo "📊 Total de capturas encontradas: $FILE_COUNT"
          
          # Verificar si hay archivos nuevos o modificados
          if git diff --quiet && git diff --staged --quiet; then
            echo "ℹ️  No hay cambios para commitear"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "✅ Se detectaron cambios"
            echo "has_changes=true" >> $GITHUB_OUTPUT
            
            echo "📊 Archivos modificados:"
            git status --porcelain
            
            echo "📈 Últimas capturas:"
            ls -la captures/*.png 2>/dev/null | tail -5 || echo "No hay archivos PNG"
          fi

      # 7. Configurar Git (solo si hay cambios)
      - name: ⚙️ Configurar Git
        if: steps.verify-changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action - Radar Capture"
          git config --local core.autocrlf false

      # 6. Limpieza automática después de captura exitosa
      - name: 🧹 Limpieza automática de capturas antiguas
        if: steps.verify-changes.outputs.has_changes == 'true'
        run: |
          cd captures
          FILE_COUNT=$(find . -name "radar_*.png" | wc -l)
          echo "📊 Archivos actuales antes de limpieza: $FILE_COUNT"
          
          if [ "$FILE_COUNT" -gt 100 ]; then
            echo "🧹 Eliminando archivos antiguos (manteniendo últimos 100)..."
            find . -name "radar_*.png" -printf '%T@ %p\n' | sort -n | head -n -100 | cut -d' ' -f2- | xargs rm -f
            NEW_COUNT=$(find . -name "radar_*.png" | wc -l)
            echo "✅ Limpieza automática completada. Archivos restantes: $NEW_COUNT"
          else
            echo "ℹ️  No es necesario limpiar (solo $FILE_COUNT archivos)"
          fi

      # 7. Commit y push (solo si hay cambios)
      - name: 💾 Commit y push cambios
        if: steps.verify-changes.outputs.has_changes == 'true'
        run: |
          # Añadir todos los cambios en captures (nuevos archivos y eliminaciones)
          git add captures/
          
          # Crear commit con timestamp
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          COMMIT_MSG="📸 Captura automática del radar - $TIMESTAMP"
          
          # Añadir información adicional al commit
          FINAL_COUNT=$(find captures -name "radar_*.png" | wc -l)
          COMMIT_MSG="$COMMIT_MSG (Total: $FINAL_COUNT capturas)"
          
          git commit -m "$COMMIT_MSG"
          
          echo "🚀 Haciendo push de los cambios..."
          git push

      # 8. Limpiar archivos antiguos (manual adicional si se solicita)
      - name: 🧹 Limpieza manual adicional
        if: github.event.inputs.cleanup_old_files == 'true' && github.event_name == 'workflow_dispatch'
        run: |
          echo "🧹 Iniciando limpieza de archivos antiguos..."
          
          # Mantener solo los últimos 100 archivos
          cd captures
          FILE_COUNT=$(find . -name "radar_*.png" | wc -l)
          
          if [ "$FILE_COUNT" -gt 100 ]; then
            echo "Archivos encontrados: $FILE_COUNT"
            echo "Eliminando archivos antiguos (manteniendo últimos 100)..."
            
            # Listar archivos por fecha y eliminar los más antiguos
            find . -name "radar_*.png" -printf '%T@ %p\n' | sort -n | head -n -100 | cut -d' ' -f2- | xargs rm -f
            
            NEW_COUNT=$(find . -name "radar_*.png" | wc -l)
            echo "✅ Limpieza completada. Archivos restantes: $NEW_COUNT"
            
            # Commit de la limpieza
            cd ..
            if ! git diff --quiet captures/; then
              git add captures/
              git commit -m "🧹 Limpieza automática - Mantenidos últimos 100 archivos"
              git push
            fi
          else
            echo "No es necesario limpiar (solo $FILE_COUNT archivos)"
          fi

      # 9. Resumen de ejecución
      - name: 📋 Resumen de ejecución
        if: always()
        run: |
          echo "=== 📋 RESUMEN DE EJECUCIÓN ==="
          echo "⏰ Timestamp: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
          echo "🔄 Trigger: ${{ github.event_name }}"
          
          if [ "${{ steps.verify-changes.outputs.has_changes }}" == "true" ]; then
            echo "✅ Estado: Captura exitosa y cambios commiteados"
            echo "📊 Total de capturas: ${{ steps.verify-changes.outputs.file_count }}"
          else
            echo "ℹ️  Estado: Ejecución completada, sin cambios"
          fi
          
          echo "📁 Contenido actual de captures/:"
          ls -la captures/ | tail -5 || echo "No hay archivos en captures/"
          
          echo "💾 Tamaño total de la carpeta captures:"
          du -sh captures/ || echo "0B"